# Legacy — original Colab export

`original_colab_export.py` is the original Google Colab notebook export
used during the study, preserved here for provenance. It is NOT meant to
be executed as a plain Python script (it contains `pip install` cell
lines and Colab-specific paths).

All API keys have been REDACTED. The maintained, runnable version of the
pipeline lives in `src/` and `scripts/`.
